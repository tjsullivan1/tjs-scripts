[project]
name = "ai-gateway-tests"
version = "0.1.0"
description = "Test scripts for the AI Gateway billing sample."
requires-python = ">=3.10"

dependencies = [
    "httpx>=0.27",
    "python-dotenv>=1.0",
    "azure-identity>=1.17",
    "azure-keyvault-secrets>=4.8",
]
